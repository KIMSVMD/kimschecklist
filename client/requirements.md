## Packages
framer-motion | Page transitions and scroll-triggered animations

## Notes
- Photo uploads go to POST /api/upload as multipart/form-data with a 'file' field. It returns { url: string }.
- Wouter connects routing.
- The standard display images for guides are currently mocked via Unsplash.
